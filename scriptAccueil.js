/*

function Playlist(tab_chanson = [], couleur = "bleu", nom="JOPlaylist", image=""){
    this.tab_chanson = tab_chanson;
    this.couleur = couleur;
    this.nom = nom;
    this.image = image;
}

function stockage(){
    // Aller chercher les données
    const nom = $('#nom').val();
    const couleur = $('#couleur').val();
    const image = $('#image').val();
    // créer un objet
    const playlist = new Playlist(couleur, nom, image);
    localStorage.setItem('playlist', JSON.stringify(playlist))
    return true;
}
*/
